# VOLT Comms — 함대 지휘망 PTT 무전기

VOLT 함대(스타시티즌) 지휘망(편대장 이상, 동시 8~10명) 전용 반이중(PTT) 음성 통신 앱입니다.
디스코드와 병행 사용하며, **한 번에 한 명만 송신**할 수 있는 무전기 규율을 서버가 강제합니다.

| 구성요소 | 기술 | 배포 형태 |
|---|---|---|
| 클라이언트 | C# / .NET 8 (WPF, 트레이 앱) | `VoltComms.exe` 단일 파일 (설치 불필요) |
| 서버 | Go | `volt-server` 리눅스 단일 바이너리 |

핵심 동작:

- **음성**: Opus 32kbps / 20ms 프레임, UDP 전송
- **제어**: WebSocket(JSON) + 공유 토큰 인증, TLS 지원
- **반이중 중재**: 서버가 발언권 토큰을 관리. 점유 중 타인이 PTT를 누르면 거부 비프음
- **stuck-PTT 페일세이프**: 연속 발언 30초 초과 또는 하트비트 끊김 시 발언권 강제 회수 (서버·클라이언트 양쪽)
- **전역 PTT**: WH_KEYBOARD_LL / WH_MOUSE_LL 저수준 훅 — 게임이 전체화면이어도 동작, 키 입력을 가로채지 않음. 마우스 사이드 버튼(Mouse4/5) 지원
- **계측**: RTT·패킷 손실을 창에 표시하고 `logs/` 폴더에 10초마다 기록

---

## 1. 빌드 결과물 다운로드 (개발 지식 불필요)

코드를 푸시할 때마다 GitHub Actions가 자동으로 빌드합니다.

1. 이 저장소의 GitHub 페이지에서 상단 **Actions** 탭 클릭
2. 왼쪽에서 **build** 워크플로 선택 → 목록 맨 위의 초록색 체크(✓)가 붙은 실행을 클릭
3. 페이지 아래 **Artifacts** 섹션에서 다운로드:
   - **`VoltComms-win-x64`** → 압축 풀면 `VoltComms.exe` (윈도우 클라이언트)
   - **`volt-server-windows-amd64`** → 압축 풀면 `volt-server.exe` (윈도우 서버)
   - **`volt-server-linux-amd64`** → 압축 풀면 `volt-server` (리눅스 서버)

> 가장 쉬운 방법은 **Releases** 페이지입니다. `v`로 시작하는 태그가 게시되면
> 위 파일들이 **로그인 없이 바로 받을 수 있는 링크**로 올라옵니다(아티팩트는 GitHub 로그인 필요).

---

## 2. 서버 실행

### 서울 VPS (리눅스)

```bash
# 1) 파일 업로드 후 실행 권한 부여
chmod +x volt-server

# 2) 실행 — 토큰은 함대원에게만 공유할 비밀번호 역할
./volt-server -token "우리함대만아는비밀토큰"
```

기본 포트: 제어 **TCP 8443**, 음성 **UDP 4001**. 방화벽/보안그룹에서 두 포트를 열어야 합니다.

자주 쓰는 옵션:

```bash
./volt-server -h                          # 전체 옵션 보기
./volt-server -token 비밀 -listen :8443 -udp-port 4001
./volt-server -token 비밀 -tls-cert cert.pem -tls-key key.pem   # TLS(wss) 활성화
```

TLS 인증서가 준비되기 전에는 평문(ws)으로 동작합니다. 이때 클라이언트 `use_tls`는 `false`로 두세요.

### 내 PC에서 호스팅 (윈도우, LAN 테스트용)

VPS가 없을 때 함대원 PC에서 직접 서버를 띄워 테스트할 수 있습니다.

1. `volt-server.exe` 를 더블클릭하면 검은 창이 뜨고 **토큰을 물어봅니다.** 함대원에게
   알려줄 비밀번호를 입력하고 Enter — 그대로 서버가 켜집니다. (창을 닫으면 서버도 꺼집니다)
2. 같은 PC에서 브라우저로 `http://localhost:8443/healthz` 를 열어 **ok** 가 보이면 정상 동작입니다.
3. 다른 사람이 접속하려면:
   - **같은 공유기(같은 와이파이)** 안이면 → 서버 PC의 내부 IP(예: `192.168.0.5`, `cmd` 에서 `ipconfig` 로 확인)
   - **외부(인터넷)** 에서면 → 서버 PC의 **공인 IP** + 공유기 **포트포워딩**(TCP 8443, UDP 4001) 필요
4. 어느 경우든 **서버 PC의 윈도우 방화벽**에서 인바운드 TCP 8443 / UDP 4001 을 허용해야 합니다.
   (처음 실행 시 방화벽 허용 팝업이 뜨면 "허용", 공용/개인 네트워크 모두 체크 권장)

> ⚠️ 서명되지 않은 exe라 실행 시 SmartScreen 경고("Windows의 PC를 보호했습니다")가 뜰 수 있습니다.
> **추가 정보 → 실행** 으로 넘어가면 됩니다.

옵션을 직접 주고 싶으면 명령 프롬프트에서:

```bat
volt-server.exe -token 비밀토큰 -listen :8443 -udp-port 4001
```

---

## 3. 클라이언트 설정과 2인 테스트

1. `VoltComms.exe`를 아무 폴더에 두고 실행하면 **설정 화면이 자동으로 열립니다**
2. 설정 화면에서 입력 (전부 앱 안에서 끝납니다, 파일 편집 불필요):
   - **주소 / 포트**: 서버 운영자에게 받은 값 (기본 포트 8443)
   - **비밀 토큰 / 콜사인**: 토큰은 서버 운영자에게, 콜사인은 다른 사람에게 보일 이름
   - **PTT 키**: [키 변경] 버튼을 누른 뒤 원하는 키나 마우스 버튼을 누르면 등록됩니다 (마우스 사이드 버튼·휠 클릭 지원, ESC 취소)
   - **마이크 / 스피커**: 드롭다운으로 선택. 말하면 입력 레벨 막대가 움직여 마이크가 잡히는지 바로 보이고, [소리 테스트]로 출력도 확인 가능
3. 저장하면 곧바로 적용 → 메인 창에 **"● 서버주소 연결됨"** 이 초록색으로 표시되면 준비 완료
4. **두 번째 사람**도 같은 방법으로 설정(콜사인만 다르게) 후 접속 — 접속자 목록에 서로 보이면 정상
5. 테스트 절차:
   - A가 PTT 키를 누른 채 말하기 → B 화면 "현재 발언자"에 A 콜사인이 뜨고(접속자 목록에 초록 점) 음성이 들림
   - A가 말하는 도중 B가 PTT를 누르면 → B에게 **거부 비프음** + "거부 — A 발언 중" 표시
   - A가 키에서 손을 떼면 발언자 표시가 사라지고, 이제 B가 송신 가능
   - 30초 이상 계속 누르고 있으면 자동으로 끊김(페일세이프) 확인

> PTT 키는 게임에서 안 쓰는 키를 고르세요. 앱은 키 입력을 가로채지 않으므로(통과시킴) 게임 키와 겹치면 양쪽에서 동시에 동작합니다.

설정은 exe 옆 `config.json` 에 저장되므로, 같은 설정을 여러 PC에 복사해 쓸 수도 있습니다.

### 문제 해결

- **연결이 안 됨**: 설정에서 서버 주소/포트/토큰 확인 → 그래도 안 되면 exe 옆 `logs/volt-날짜.log` 파일 확인
- **마이크가 안 잡힘**: 설정 화면에서 말해 보며 입력 레벨이 움직이는 마이크를 선택. 윈도우 설정 → 개인 정보 → 마이크 권한도 확인
- **연결은 되는데 음성이 안 감**: 서버 방화벽에서 **UDP 4001** 이 열려 있는지 확인 (제어 TCP 8443과 별개)
- 창을 닫으면 종료가 아니라 **트레이로 내려갑니다**(트레이 아이콘이 수신 시 초록, 송신 시 빨강으로 변함). 완전 종료는 트레이 아이콘 우클릭 → 종료

---

## 4. 저장소 구조

```
server/   Go 서버 (발언권 중재 + UDP 음성 릴레이)
client/   .NET 8 WPF 클라이언트 (트레이 앱)
docs/     프로토콜 명세
.github/  자동 빌드 워크플로
```

프로토콜 상세는 [docs/PROTOCOL.md](docs/PROTOCOL.md) 참조.

## 5. 직접 빌드 (선택)

```bash
# 서버 (Go 1.23+)
cd server && go build -o volt-server .

# 클라이언트 (윈도우, .NET 8 SDK)
dotnet publish client/VoltComms/VoltComms.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -o dist
```
